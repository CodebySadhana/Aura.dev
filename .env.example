# Leave empty for clearly marked local-development auth; set in production.
AURA_API_TOKEN=
AURA_CORS_ORIGINS=http://localhost:5173
# Local Vite proxy uses /api/v1 automatically. Set this only for a deployed backend.
VITE_API_URL=
